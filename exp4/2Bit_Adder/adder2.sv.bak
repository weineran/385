module full_adder (input a1,a2,b1,b2,cin,
						 output s1, s2, cout, c1);
						 
	assign s1 = a1^b1^cin;
	assign c1 = (a1&b1)|(b1&cin)|(a1&cin);
	assign s2 = a2^b2^c1;
	assign cout = (a2&b2)|(b2&c1)|(a2&c1);
	
endmodule
